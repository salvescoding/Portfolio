<div class="col-sm-8 blog-main">

  <h2>
    <%= @topic.title %>
  </h2>

  <% @topic.blogs.each do |blog| %>
    <h3><%= link_to blog.title, blog %></h3>
  <p>
    <%= blog.body.first(300) %>...
  </p>
  <% end %>
</div>
