<div class="container portfolio-container">
  <div class="row">
    <div class="col-md-7">
      <%= image_tag @work.main_image.url if @work.main_image.url %>
      <br>
    </div>
    <div class="col-md-5">
     <h4>
       <%= @work.title %>
     </h4>
      <p>
        <%= @work.body %>
      </p>
      <hr>
      <h5>Technologies used:</h5>
        <% @work.technologies.each do |t| %>
        <p>
          <%= t.name %>
        </p>
        <% end %>
      <%= link_to 'Edit', edit_work_path(@work), class: 'btn btn-outline-secondary btn-sm' if logged_in?(:site_admin) %>
    </div>
  </div>
</div>


